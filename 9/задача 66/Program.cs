/*===========================================================================================================
ПРОГРАММА НАХОДИТ СУММУ НАТУРАЛЬНЫХ ЭЛЕМЕНТОВ В ПРОМЕЖУТКЕ ОТ M ДО N, ЧЕРЕЗ РЕКУРСИЮ
=============================================================================================================*/
#pragma warning disable
int SumRec(int M, int N)
{
    if (N >= M) return N + SumRec(M, N - 1);
    else return 0;
}
Console.Write($"{SumRec(1, 15)}");